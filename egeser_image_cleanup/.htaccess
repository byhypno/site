Options -Indexes
<IfModule mod_authz_core.c>
  <FilesMatch "\.(json|txt|log)$">
    Require all denied
  </FilesMatch>
</IfModule>
<IfModule !mod_authz_core.c>
  <FilesMatch "\.(json|txt|log)$">
    Order allow,deny
    Deny from all
  </FilesMatch>
</IfModule>
